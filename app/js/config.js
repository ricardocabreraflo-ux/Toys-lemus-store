// Fill these in once the Supabase project exists (Project Settings > API).
// SUPABASE_ANON_KEY is the public "anon" key — safe to ship in client code,
// because Row Level Security on the `products` table is what actually
// controls who can write. Never put the service_role key here.
export const SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR-ANON-PUBLIC-KEY';
